# AI 知识库 · 编码规范

## 要做什么

- **Python 格式**：black · line-length=100
- **TypeScript**：strict: true + noUncheckedIndexedAccess: true
- **文档**：所有公开函数必须有文档
  - Python：Google 风格 docstring
  - TS：TSDoc · @param / @returns 必填
  - private 函数不强制，但命名必须自解释
- **行宽统一**：Python 100 · TS 100
- **导入顺序**：Python isort，分 stdlib / 3rd-party / local 三组
- **命名规约**：
  - Python：snake_case
  - TS：camelCase
  - 类：PascalCase
  - 文件：kebab-case
- **AI 生成标识**：AI 生成的代码必须有 `# Generated by <agent-name> on <date>` 注释
- **AI 修改 commit**：AI 修改过的文件 commit message 用 `[AI]` 前缀

## 不做什么

- **魔法字符串**：代码中的硬编码字符串必须提到模块顶部 UPPER_SNAKE 常量
  - 例外：单次使用的用户消息、测试数据
- **禁止 TODO**：不允许 TODO / FIXME / XXX / HACK 直接进 main（只允许 NOTE）

## 边界 & 验收

### 单测覆盖率分层

| 层级 | 目录 | 覆盖率要求 |
|------|------|-----------|
| 业务逻辑层 | services/, handlers/ | ≥ 90% |
| 工具层 | utils/, helpers/ | ≥ 80% |
| 边界适配层 | adapters/, clients/ | ≥ 50%（mock 难写） |
| 总平均 | - | ≈ 80% |

### lint 检查工具链

- **Python**：black + ruff + mypy
- **TypeScript**：eslint + tsc strict

## 怎么验证

- `make lint`：一键跑全部格式检查（black / ruff / eslint）
- **CI**：每个 PR 跑 lint + 单测覆盖率检查
- `make test-coverage`：输出分层覆盖率报告
- **pre-commit hook**：拦截不符合规范的 commit
- **code review**：`[AI]` commit 必须经过人工 review 才能 merge